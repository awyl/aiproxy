{
  "name": "aiproxy",
  "version": "0.2.6",
  "description": "Setup-once LLM gateway: one OpenAI-compatible endpoint over many upstreams, plus MCP multiplexer, embeddings, and the pi provider extension",
  "keywords": ["pi-package"],
  "type": "module",
  "private": true,
  "pi": {
    "extensions": ["agent/pi/extensions/aiproxy"]
  }
}
